# nullstate Docker Compose Exposed Admin Demo

Scaffolded Docker Compose digital-twin scenario.

This fixture intentionally exposes an admin service on `0.0.0.0:8080`. The detector/exploit path is scaffolded for the hackathon roadmap; the working V1 exploit loop remains `azure-public-blob`.
